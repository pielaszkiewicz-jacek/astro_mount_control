# GUI

This directory is intentionally **empty**.

The Qt desktop GUI was removed from the project (documented in
`docs/en/qt_gui.md` as unavailable). The current user interface is the **web
SPA** served by the proxy server (`web/`).

Any future GUI work should be placed here or in a dedicated subdirectory.
