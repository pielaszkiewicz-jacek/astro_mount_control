// Tmc5160Derotator moved to a header (include/hal/derotator_tmc5160.h) so it
// can be shared with the derotator factory and unit tests. All methods are
// defined in-class (inline), so including the header here compiles the class
// into the astro_mount_core library.
#include "hal/derotator_tmc5160.h"
